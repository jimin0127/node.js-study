<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
</head>
<body>
    <a href="/write">글쓰기</a>
    <h1>BOARD LIST</h1>

    <table border='1'>
        <tr>
            <td>번호</td>
            <td>작성자</td>
            <td>제목</td>
            <td>첨부파일</td>
            <td>조회수</td>
            <td>등록일</td>
        </tr>
        
        <% rows.forEach((list) => { %>
        <tr>
            <td><%= list.id %></td>
            <td><%= list.name %></td>
            <td><a href="/read/<%= list.id%>"><%= list.title %></a></td>
            <td><a href="/down/<%= list.file_path%>"><%= list.file_name %></a></td>
            <td><%= list.hit %></td>
            <td><%= list.regdate %></td>
        </tr>
        <%})%>
    </table>

    <form action="/search" method="post">
        <select name="type">
            <option value="name">작성자</option>
            <option value="title">제목</option>
            <option value="content">내용</option>
        </select>
        <input type="text" name="value">
        <input type="submit" value="검색">
    </form>

    
    <% if(user != null) {%>
        <a href="/logout">로그아웃</a>
    <% } else { %>
        <a href="/login">로그인</a>
        <a href="/signup">회원가입</a>
    <% } %>

</body>
</html>
